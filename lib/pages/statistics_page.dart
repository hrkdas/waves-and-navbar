import 'package:flutter/material.dart';

class StatsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Stats Page',textScaleFactor: 2.0,),
    );
    throw UnimplementedError();
  }

}